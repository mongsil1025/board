server:
  port: 8080

datasource:
    url: jdbc:h2:mem:testdb

spring:
  h2:
    console:
      enabled: true

property:
    test:
        name: property depth test

propertyTest: test

propertyTestList: a,b,c

fruit:
    list:
      - name: banana
        color: yellow
      - nmae: apple
        color: red
      - name: water melon
        color: green